# ============================================
# Docker构建时忽略的文件
# SRE最佳实践：减小构建上下文，加快构建速度
# ============================================

# ============================================
# 为什么需要.dockerignore？
# ============================================
# 1. 减小构建上下文 - 传输给Docker daemon的文件更少
# 2. 加快构建速度 - 不需要复制不必要的文件
# 3. 减小镜像体积 - 避免复制大文件
# 4. 提高安全性 - 避免敏感文件进入镜像

# ============================================
# Python相关
# ============================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/
*.egg

# ============================================
# 虚拟环境
# 面试问题：为什么要忽略虚拟环境？
# 答：容器内会重新安装依赖，本地虚拟环境没用
# ============================================
venv/
env/
ENV/
.venv/

# ============================================
# IDE和编辑器
# ============================================
.vscode/
.idea/
*.swp
*.swo
*.swn
.vs/

# ============================================
# Git相关
# ============================================
.git/
.gitignore
.gitattributes

# ============================================
# 测试和覆盖率
# ============================================
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# ============================================
# 文档（如果不需要打包进镜像）
# ============================================
*.md
docs/
README.md

# ============================================
# Docker相关
# 避免递归复制
# ============================================
Dockerfile
.dockerignore
docker-compose.yml

# ============================================
# Kubernetes配置
# ============================================
k8s/
*.yaml
*.yml

# ============================================
# Terraform配置
# ============================================
terraform/
*.tf
*.tfstate
*.tfvars

# ============================================
# 临时文件和日志
# ============================================
*.log
*.tmp
tmp/
.DS_Store
Thumbs.db

# ============================================
# 敏感信息（非常重要！）
# 面试考点：绝对不能把这些打包进镜像
# ============================================
.env
.env.local
*.pem
*.key
secrets/
credentials.json

# ============================================
# 大文件（数据、模型等）
# ============================================
data/
*.csv
*.db
*.sqlite
models/

# ============================================
# CI/CD配置
# ============================================
.github/
.gitlab-ci.yml
Jenkinsfile

# ============================================
# 面试高频问题
# ============================================
# Q: .dockerignore和.gitignore有什么区别？
# A: 
# - .dockerignore：控制哪些文件不复制到Docker镜像
# - .gitignore：控制哪些文件不提交到Git仓库
# - 两者用途不同，但经常有重叠（如__pycache__）
#
# Q: 如果没有.dockerignore会怎样？
# A:
# - 构建上下文会包含所有文件（可能几GB）
# - 构建速度慢（需要传输大量文件到Docker daemon）
# - 可能泄露敏感信息（如.env文件）
# - 镜像体积变大
#
# Q: 如何查看构建上下文大小？
# A:
# 在docker build时会显示：
# "Sending build context to Docker daemon  XX.XXkB"
# 
# 优化前可能是几百MB，优化后应该只有几MB
